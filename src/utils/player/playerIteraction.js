export default class PlayerIteraction {
    constructor(scene, callback) {
        this.scene = scene
        this.callback = callback
        this.iteraction()
    }

    iteraction() {
        const 
    }

}